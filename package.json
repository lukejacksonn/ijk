{
  "name": "ijk",
  "version": "0.21.0",
  "description": "Transforms arrays into virtual dom trees",
  "main": "index.js",
  "module": "index.js",
  "scripts": {
    "test": "tead --coverage",
    "release": "npm test && git commit -am $npm_package_version && git tag $npm_package_version && git push origin master --tags && npm publish"
  },
  "keywords": [
    "vdom",
    "html",
    "hyperscript",
    "hyperapp",
    "ultradom",
    "preact"
  ],
  "author": "Luke Jackson",
  "license": "MIT",
  "devDependencies": {
    "tead": "^0.4.3"
  }
}
